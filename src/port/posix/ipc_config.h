/* ipc_config.h — POSIX/default IPC configuration shim.
 *
 * Selected through CMake include directories. POSIX has no Kconfig layer,
 * so it forwards directly to the public defaults.
 */
#pragma once

#include "ipc_defaults.h"
