CONFIG_ACTOR=y

# Keep the sample small and explicit.  These values must cover the largest
# actor stack/queue requested in src/main.c.
CONFIG_ACTOR_PAYLOAD_SIZE=32
CONFIG_ACTOR_MAX_REGISTRATIONS=4
CONFIG_ACTOR_MAX_SUBSCRIPTIONS=4
CONFIG_ACTOR_MAX_ACTORS=1
CONFIG_ACTOR_MAX_STACK_SIZE=1024
CONFIG_ACTOR_MAX_QUEUE_DEPTH=4
